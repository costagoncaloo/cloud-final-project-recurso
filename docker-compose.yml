services:
  catalog-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: catalog_db
      POSTGRES_USER: catalog_user
      POSTGRES_PASSWORD: catalog_password
    ports:
      - "5432:5432"
    volumes:
      - catalog-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U catalog_user -d catalog_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  order-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: order_db
      POSTGRES_USER: order_user
      POSTGRES_PASSWORD: order_password
    ports:
      - "5433:5432"
    volumes:
      - order-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U order_user -d order_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  catalog-service:
    build: ./services/catalog-service
    environment:
      DATABASE_URL: jdbc:postgresql://catalog-db:5432/catalog_db
      DATABASE_USERNAME: catalog_user
      DATABASE_PASSWORD: catalog_password
    ports:
      - "8081:8081"
    depends_on:
      catalog-db:
        condition: service_healthy

  order-service:
    build: ./services/order-service
    environment:
      DATABASE_URL: jdbc:postgresql://order-db:5432/order_db
      DATABASE_USERNAME: order_user
      DATABASE_PASSWORD: order_password
    ports:
      - "8082:8082"
    depends_on:
      order-db:
        condition: service_healthy
      catalog-service:
        condition: service_started

  notification-service:
    build: ./services/notification-service
    ports:
      - "8083:8083"

volumes:
  catalog-db-data:
  order-db-data:
